# routes/

Define os endpoints da API e qual controller cada um chama (ex.: `POST /api/scores` → `scores.controller.js`).

Arquivo só de "mapeamento" — nenhuma lógica aqui além de ligar caminho HTTP + método a uma função de controller.
