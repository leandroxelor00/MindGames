# components/ScoreBoard/

Componente que exibe placar/estatísticas de uma partida (pontos, tentativas, acertos) de forma padronizada — usado dentro de qualquer jogo.

Só recebe os números prontos via props; não calcula nada sozinho. O cálculo de progresso/estatística fica nos hooks de cada jogo ou em `utils/`.
