# components/Button/

Botão genérico reutilizável em toda a aplicação (jogos, formulários, dashboard, settings).

Não deve conter lógica de negócio nenhuma — só aparência e comportamento visual (variações de estilo, estado desabilitado, etc.), recebendo o que fazer via props (`onClick`).
