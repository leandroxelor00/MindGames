# games/sea-memory/

Memória das Profundezas — V0.13 do roadmap. Memorizar posições de criaturas marinhas que aparecem brevemente na tela.

Contém `SeaMemory.jsx`, `useSeaMemory.js` e uma subpasta `assets/` com os SVGs das criaturas usadas só por este jogo (diferente de `public/assets/`, que é pra estáticos gerais).
